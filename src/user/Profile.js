import React from 'react'
import Base from "../core/Base"

const = Profile()=> {
    return (
        <Base title="profile section">
         <h1> we are in Profile section.</h1>
        </Base>
    )
}

export default Profile;